name: Build

on: 
  push:
    branches: [main]

jobs:
  dependencies:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Install dependencies
        run: yarn

      - name: Linters
        run: yarn linters

      - name: Build
        run: yarn build

      - name: Deploy
        run: yarn deploy
